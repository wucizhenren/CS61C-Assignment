#test
    addi sp,sp,-4
    sw a1,0(sp)
    mv a1,a0
    jal print_int
    lw a1,0(sp)
    addi sp,sp,4

java -jar venus.jar 
TestMain_test0.s inputs/simple0/bin/m0.bin inputs/simple0/bin/m1.bin inputs/simple0/bin/inputs/input0.bin outputs/test_basic_main/student0.bin

#test
    lw a1,4(s0)
    jal print_int
    lw a1,9
    jal print_char
    lw a1,8(s0)
    jal print_int

s3 col m1 4
s4 row input 4

目前错误，在run layer，要么是maxmul，要么就是后面的计算导致的错误。

lw a0,16(s0)
    mv a1,s5
    lw a2,4(s2)
    lw a3,8(s4)